--- metis-4.0.3/Lib/ccgraph.c	2019-02-07 18:21:15.424298005 +0100
+++ metis-4.0.3/Lib/ccgraph.c	2019-02-07 18:22:47.296662334 +0100
@@ -159,7 +159,8 @@
             break;
         }
       }
-      if (jj >= 0 && cadjncy[jj] == cnvtxs) { /* This 2nd check is needed for non-adjacent matchings */
+      /* added condition jj < nedges as suggested in https://github.com/coin-or/Ipopt/issues/208#issuecomment-460986757 */
+      if (jj >= 0 && jj < nedges && cadjncy[jj] == cnvtxs) { /* This 2nd check is needed for non-adjacent matchings */
         cadjwgtsum[cnvtxs] -= cadjwgt[jj];
         cadjncy[jj] = cadjncy[--nedges];
         cadjwgt[jj] = cadjwgt[nedges];
--- metis-4.0.3/Lib/compress.c	2026-01-31 12:00:00.000000000 +0100
+++ metis-4.0.3/Lib/compress.c	2026-01-31 12:00:00.000000000 +0100
@@ -100,10 +100,14 @@
     for (i=0; i<nvtxs; i++)
       graph->label[i] = i;
   }
   else { /* Ok, form the compressed graph  */
-    cnedges = 0;
-    for (i=0; i<cnvtxs; i++) {
-      ii = cind[cptr[i]];
-      cnedges += xadj[ii+1]-xadj[ii];
+    /* Count edges from ALL vertices in each compressed group, not just the first.
+       This fixes a buffer overflow when multiple vertices are merged. */
+    cnedges = 0;
+    for (i=0; i<cnvtxs; i++) {
+      for (j=cptr[i]; j<cptr[i+1]; j++) {
+        ii = cind[j];
+        cnedges += xadj[ii+1]-xadj[ii];
+      }
     }

     /* Allocate memory for the compressed graph*/
--- metis-4.0.3/Lib/proto.h	2026-02-02 12:00:00.000000000 +0100
+++ metis-4.0.3/Lib/proto.h	2026-02-02 12:00:00.000000000 +0100
@@ -432,7 +432,9 @@
 idxtype *idxsmalloc(int, idxtype, char *);
 void *GKmalloc(int, char *);
 #endif
-/*void GKfree(void **,...); */
+/* CRITICAL: This prototype MUST be declared for ARM64 compatibility.
+   Without it, the variadic calling convention is not used, causing heap corruption on Apple Silicon. */
+void GKfree(void **,...);
 int *iset(int n, int val, int *x);
 idxtype *idxset(int n, idxtype val, idxtype *x);
 float *sset(int n, float val, float *x);
